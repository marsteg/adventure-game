# Global Style Configuration for Asset Generator
# This file defines the visual style for ALL generated assets in your game

# ============================================================================
# MASTER STYLE DEFINITION
# ============================================================================
# This is applied to EVERY asset generated. Be as specific as possible!

master_style:
  # Primary style descriptors (keep SHORT for SDXL - max 77 tokens!)
  art_style: "2D cartoon, hand-drawn, flat colors, cel-shaded"

  # Detail level
  detail_level: "simple, clean lines"

  # Color palette guidance
  color_style: "vibrant colors, bold outlines"

  # What to AVOID (very important for consistency!)
  negative_keywords: "3D render, photorealistic, CGI, blurry, low quality"

  # Additional style notes (SHORTENED)
  additional: "adventure game style"

# ============================================================================
# ASSET-SPECIFIC STYLE OVERRIDES
# ============================================================================
# Fine-tune style for specific asset types while keeping master style

npcs:
  style: "full body sprite, front view"
  negative: "side view, dynamic pose"
  additional: "clear silhouette"

rooms:
  style: "interior background, wide angle"
  negative: "character in scene, portrait"
  additional: "atmospheric environment"

items:
  style: "game item icon, centered"
  negative: "multiple objects, text"
  additional: "inventory style"

doors:
  style: "front-facing door"
  negative: "open door"
  additional: "game asset"

actions:
  style: "single object, centered"
  negative: "multiple objects, text"
  additional: "clear icon"

# ============================================================================
# PROMPT ENHANCEMENT SETTINGS
# ============================================================================

enhancement:
  # Strength of style enforcement (0.0 to 1.0)
  style_weight: 0.9

  # Add these quality boosters (SHORTENED)
  quality_boosters:
    - "high quality"
    - "clean artwork"

  # Always exclude these (SHORTENED)
  universal_negatives:
    - "blurry"
    - "low quality"
    - "distorted"
    - "watermark"

# ============================================================================
# REFERENCE EXAMPLES (Optional)
# ============================================================================
# If you have example images that match your desired style, note them here
# The tool can reference these in prompts

reference_style_notes: |
  Think: Classic LucasArts adventure games
  - Bold outlines
  - Flat cel-shading
  - Cartoon proportions
  - Clear readable shapes
  NOT: Modern 3D games, realistic renders, Pixar-style 3D animation

# ============================================================================
# TIPS FOR CUSTOMIZATION
# ============================================================================
# 1. Be VERY specific in master_style.art_style
# 2. Use master_style.negative_keywords to exclude unwanted styles
# 3. Add reference game names in additional fields
# 4. Experiment with style_weight if results aren't matching
# 5. Update this file anytime you want to change your game's look
